import 'package:flutter/material.dart';

class BackgroundEmptyView extends StatelessWidget {
  const BackgroundEmptyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: double.infinity,
      height: 700,
    );
  }
}
